CREATE TABLE IF NOT EXISTS Zaaa_impound (
    id INT AUTO_INCREMENT PRIMARY KEY,
    reason VARCHAR(50),
    model VARCHAR(50),
    plate VARCHAR(20),
    pengirim VARCHAR(20),
    status VARCHAR(20)
);
